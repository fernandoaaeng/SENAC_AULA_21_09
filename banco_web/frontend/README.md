# NexusBank UI

Front Angular do laboratorio (Internet Banking e backoffice).

Na raiz do projeto: `docker compose up --build` (porta 8080).

Em desenvolvimento: `npm start` (porta 4200, proxy para a API em `http://127.0.0.1:8002`).
